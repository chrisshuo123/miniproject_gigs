// Example 1:
let word1 = 'Chris';
let word2 = 'Shuo';

// the output in the classic ways:
// const fullName = word1 + ' ' + word2;
// console.log(fullName);

// In Template Literals, it allows us to use Back Ticks
const fullName = `${word1} ${word2}`;
console.log(fullName);