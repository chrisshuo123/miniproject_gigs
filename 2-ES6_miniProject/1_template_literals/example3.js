// Example 3: Enter Header
// we can use '\n' to query an enter space
// let example = 'Hello \n' + 'world';
// console.log(example);

// But what about if it fails to enter?
let example = 'Hello \n' + 'world';
console.log(example);
document.getElementById('example').innerText = example;