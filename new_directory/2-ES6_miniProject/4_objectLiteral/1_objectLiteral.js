function addressMaker(city, state) { // let's say this is key
    const newAddress = {city, state}; // and this is value
    // the big pic =>   key , key.  Where key and value are the same

    console.log(newAddress);
}

addressMaker('Austin', 'Texas');

// Prinsip Simplenya: Key -> value